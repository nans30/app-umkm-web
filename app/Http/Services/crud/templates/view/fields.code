<div class="col-sm-12">
    <div class="mb-3">
        <label>Title <span class="text-danger">*</span></label>
        <input class="form-control" type="text" name="title"
               value="{{ isset($$modulName->title) ? $$modulName->title : old('title') }}"
               placeholder="Enter Title">
        @error('title')
            <span class="text-danger d-block"><strong>{{ $message }}</strong></span>
        @enderror
    </div>

    <div class="mb-3">
        <label>Meta Title</label>
        <input class="form-control" type="text" name="meta_title"
               value="{{ isset($$modulName->meta_title) ? $$modulName->meta_title : old('meta_title') }}"
               placeholder="Enter Meta Title">
        @error('meta_title')
            <span class="text-danger d-block"><strong>{{ $message }}</strong></span>
        @enderror
    </div>

    <div class="mb-3">
        <label>Meta Description</label>
        <textarea class="form-control" name="meta_description"
                  placeholder="Enter Meta Description">{{ isset($$modulName->meta_description) ? $$modulName->meta_description : old('meta_description') }}</textarea>
        @error('meta_description')
            <span class="text-danger d-block"><strong>{{ $message }}</strong></span>
        @enderror
    </div>

    <div class="mb-3">
        <label>Meta Image</label>
        <input class="form-control" type="file" name="meta_image">
        @isset($$modulName)
            <div class="mt-3 comman-image">
                @foreach ($$modulName->getMedia('meta_image') as $image)
                    <img src="{{ $image->getUrl() }}" alt="Image" class="img-thumbnail img-fix" width="100" height="100">
                @endforeach
            </div>
        @endisset
        @error('meta_image')
            <span class="text-danger d-block"><strong>{{ $message }}</strong></span>
        @enderror
    </div>

    <div class="mb-3">
        <label>Status <span class="text-danger">*</span></label>
        <select class="form-select" name="status">
            <option value="" disabled hidden selected>-- Select Status --</option>
            <option value="1" {{ (isset($$modulName) && $$modulName->status == 1) || old('status') == '1' ? 'selected' : '' }}>
                Active
            </option>
            <option value="0" {{ (isset($$modulName) && $$modulName->status == 0) || old('status') == '0' ? 'selected' : '' }}>
                Inactive
            </option>
        </select>
        @error('status')
            <span class="text-danger d-block"><strong>{{ $message }}</strong></span>
        @enderror
    </div>

    <div class="text-end">
        <a href="{{ route($routePrefix . '.index') }}" class="btn btn-danger">
            <i class="ti ti-arrow-left me-1"></i> Cancel
        </a>
        <button type="submit" class="btn btn-primary">
            <i class="ti ti-device-floppy me-1"></i> Save
        </button>
    </div>
</div>
